# Optional — enables the AI sentiment + summary feature.
# Get a free key at https://aistudio.google.com  (Google AI Studio).
# Leave unset and the app runs fine; AI columns just stay empty.
# Copy this file to server/.env and fill in the key.
GEMINI_API_KEY=
